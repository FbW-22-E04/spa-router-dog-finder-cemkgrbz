import React from "react";
import ReactDOM from "react-dom/client";
import DogsFinder from "./components/DogsFinder.js";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <DogsFinder />
  </React.StrictMode>
);
